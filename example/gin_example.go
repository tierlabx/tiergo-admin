package main

func main() {

	// 初始化配置
	/* config := config.Config{}
	config.InitConfig()
	// 初始化数据库
	sqlDB, gormDB := db.InitDB(config)
	defer sqlDB.Close()

	router := gin.Default()

	router.Run(":88") */
}
